// 15) Ler um valor e escrever se é positivo ou negativo (considere o valor zero como positivo).

var numero = parseInt(prompt("Digite um número:"))

var resultado = numero >= 0 ? 'Número positivo.' : 'Número negativo.';

alert(resultado)